"""scenes.py — claude-liam-ai-explainer

Pure-Remotion reel. All 11 beats use Remotion patterns registered in Root.tsx.
No Manim Scene subclasses needed.
"""
import sys, pathlib
sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[2] / "runtime" / "manim"))
from animated_graphics import *
